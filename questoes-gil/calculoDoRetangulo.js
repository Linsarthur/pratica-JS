//calculo da área e do perimetro do retangulo.

const base = 11
const altura = 7

const area = base * altura
const perimetro = 2 * (base + altura)

console.log(`Nesse retangulo de base ${base}cm e de altura ${7}cm. Temos a área com ${area}cm e o perímetro com ${perimetro}cm.`);
